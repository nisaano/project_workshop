```markdown
# University Project - Backend API

## Описание проекта

Backend-часть веб-приложения с интеграцией OpenAI и локального AI. Предоставляет REST API для регистрации пользователей, аутентификации, управления профилями и работы с AI-функционалом.

## 🛠 Технологии

- **FastAPI** - современный фреймворк для построения API
- **PostgreSQL** - реляционная база данных
- **SQLAlchemy** - ORM для работы с базой данных
- **Alembic** - система миграций базы данных
- **JWT** - аутентификация через JSON Web Tokens
- **Pydantic** - валидация данных и сериализация
- **Uvicorn** - ASGI-сервер для запуска приложения

## 📁 Структура проекта

```
backend/
├── app/
│   ├── core/                 # Основные настройки и утилиты
│   │   ├── config.py        # Конфигурация приложения
│   │   ├── dependencies.py  # Зависимости FastAPI
│   │   └── security.py      # Функции безопасности
│   ├── db/                  # Работа с базой данных
│   │   ├── migrations/      # Миграции Alembic
│   │   │   ├── versions/   # Файлы миграций
│   │   │   ├── env.py
│   │   │   ├── base.py
│   │   │   └── session.py
│   │   └── models/          # Модели данных
│   │       ├── user.py
│   │       └── note.py
│   ├── routers/             # Маршруты API
│   │   ├── auth.py
│   │   └── users.py
│   ├── schemas/             # Pydantic схемы
│   │   └── user.py
│   ├── services/            # Бизнес-логика
│   │   └── auth_service.py
│   └── main.py              # Точка входа приложения
├── .env                     # Переменные окружения
├── requirements.txt         # Зависимости Python
├── create_tables.py         # Скрипт создания таблиц
└── test_db.py              # Тест подключения к БД
```

## ⚙️ Установка и запуск

### Предварительные требования

- Python 3.8+
- PostgreSQL 12+
- pip (менеджер пакетов Python)

### Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd ProjectUniversity/backend
```

2. **Создайте виртуальное окружение**
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/MacOS
# или
.venv\Scripts\activate     # Windows
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Настройте базу данных**
```bash
# Создайте базу данных в PostgreSQL
createdb project_db

# Или через psql
psql -U postgres -c "CREATE DATABASE project_db;"
```

5. **Настройте переменные окружения**
Создайте файл `.env` в папке `backend`:
```env
DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/project_db
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4
LOCAL_AI_URL=http://localhost:5000
FRONTEND_URL=http://localhost:3000
```

6. **Создайте таблицы в базе данных**
```bash
python create_tables.py
```

7. **Запустите сервер**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Приложение будет доступно по адресу: `http://localhost:8000`

## 📚 API Endpoints

### Аутентификация

- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему (получение JWT токена)
- `GET /auth/me` - Информация о текущем пользователе
- `POST /auth/verify-token` - Проверка валидности токена

### Пользователи

- `GET /users/me` - Профиль текущего пользователя
- `PUT /users/me` - Обновление профиля пользователя
- `GET /users/` - Список всех пользователей (только для администраторов)
- `GET /users/{user_id}` - Пользователь по ID (только для администраторов)
- `DELETE /users/{user_id}` - Удаление пользователя (только для администраторов)
- `PATCH /users/{user_id}/activate` - Активация/деактивация пользователя

## 🔐 Аутентификация

Система использует JWT (JSON Web Tokens) для аутентификации:

1. **Регистрация**: пользователь создает учетную запись
2. **Логин**: пользователь получает access token
3. **Доступ к API**: токен передается в заголовке `Authorization: Bearer <token>`

## 🗄 База данных

### Модели данных

**User (пользователь):**
- `id` - уникальный идентификатор
- `username` - имя пользователя (уникальное)
- `email` - email (уникальный)
- `hashed_password` - хешированный пароль
- `is_active` - статус активности
- `is_admin` - права администратора
- `created_at`, `updated_at` - временные метки

**Note (заметка):**
- `id` - уникальный идентификатор
- `title` - заголовок заметки
- `content` - содержание заметки
- `owner_id` - ID владельца (связь с User)
- `created_at`, `updated_at` - временные метки

### Миграции

Для управления изменениями схемы базы данных используется Alembic:

```bash
# Создание новой миграции
alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1
```

## ⚙️ Конфигурация

Настройки приложения загружаются из переменных окружения через `pydantic-settings`:

- **DATABASE_URL** - строка подключения к PostgreSQL
- **SECRET_KEY** - секретный ключ для JWT
- **ALGORITHM** - алгоритм шифрования JWT
- **ACCESS_TOKEN_EXPIRE_MINUTES** - время жизни токена
- **OPENAI_API_KEY** - ключ API OpenAI
- **LOCAL_AI_URL** - URL локального AI сервиса
- **FRONTEND_URL** - URL фронтенд-приложения

## 🧪 Тестирование

### Тест подключения к базе данных
```bash
python test_db.py
```

### Тестирование API
1. Запустите сервер
2. Откройте `http://localhost:8000/docs` для доступа к Swagger UI
3. Протестируйте endpoints через интерактивную документацию

## 🔧 Утилиты

- `create_tables.py` - создание таблиц в базе данных
- `test_db.py` - тестирование подключения к БД
- `alembic/` - управление миграциями базы данных

## 👥 Разработчики

Backend-часть разработана как основа для полнофункционального веб-приложения с AI-интеграцией. Готова к подключению фронтенда и AI-модулей.

---
